/**
 * Single source of truth for the module id.
 * Import this constant wherever the module id is referenced — never use the string literal.
 * @type {string}
 */
export const MODULE_ID = "click-adventure";
